type: object
properties:
  code:
    type: string
    description: Machine-readable error code
    enum:
      - UNAUTHORIZED
  message:
    type: string
    description: Human-readable error description
required:
  - code
  - message
