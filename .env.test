# Test Environment Configuration
# Used by integration tests running sequentially

# Database URL - no schema parameter (schema is set programmatically in tests)
DATABASE_URL="postgresql://postgres@localhost:5432/test_financial_planner_local_db"

# JWT Secrets - Use different secrets for testing
JWT_ACCESS_SECRET="test-access-secret-min-32-chars-long-for-jwt"
JWT_REFRESH_SECRET="test-refresh-secret-min-32-chars-long-for-jwt"

# Environment
NODE_ENV="test"

# Disable logging noise during tests
LOG_LEVEL="error"
