# Test Environment Configuration
# Used by integration tests with schema-based isolation

# Database URL - Each Vitest worker gets its own schema (test_worker_1, test_worker_2, etc.)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/test_db"

# JWT Secrets - Use different secrets for testing
JWT_ACCESS_SECRET="test-access-secret-min-32-chars-long-for-jwt"
JWT_REFRESH_SECRET="test-refresh-secret-min-32-chars-long-for-jwt"

# Environment
NODE_ENV="test"

# Disable logging noise during tests
LOG_LEVEL="error"
